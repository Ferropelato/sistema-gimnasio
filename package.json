{
  "name": "center-gym-sistema",
  "version": "1.0.0",
  "description": "Sistema de gestión para Center Gym",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node server/index.mjs",
    "build": "vite build",
    "start": "cross-env NODE_ENV=production node server/index.mjs",
    "preview": "cross-env NODE_ENV=production node server/index.mjs",
    "deploy:rules": "firebase deploy --only database",
    "test": "vitest run"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "firebase-tools": "^14.0.0",
    "vite": "^5.0.0",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^4.21.2",
    "express-rate-limit": "^8.3.2",
    "firebase": "^12.10.0",
    "helmet": "^8.0.0",
    "serverless-http": "^3.2.0"
  }
}
